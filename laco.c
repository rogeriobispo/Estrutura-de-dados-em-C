#include <stdio.h>
int main() {
int cont = 1;
    while(cont <= 10)
    {
        printf("não vou reclamar numca mais \n");
        cont++;
    }

    for(cont = 1; cont <= 10; cont++)
    {
        printf("não vou entrar no wpp");
    }
}
